// Declare uma variável contendo uma string
var nome = 'Lucas';

// Declare uma variável contendo um número dentro de uma string
var numero = '2002';

// Declare uma variável com a sua idade;
var idade = 20;

// Declare duas variável, uma com seu nome e a outra com seu sobrenome e some as mesmas
var sobrenome = 'Mendes';
var some = `${nome} ${sobrenome}`;

// Coloque a seguinte frase em uma variável: It's time
var frase = 'It\'s time';

// Verifique o tipo de variável que contem o seu nome;
var verificaTipoNome = typeof nome;
console.log(verificaTipoNome);